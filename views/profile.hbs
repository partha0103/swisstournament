{{>header}}
    <h1 class="col-md-12 text-center text-info">Add Tournament</h1>
    <div class="row">
        <div class="col-md-4 input-group col-md-offset-4">
        <input type="text" name="players" class="form-control in_tour ">
            <span class="input-group-btn">
                <button class="btn btn-info btn-primary tournament" type="button">Add</button>
            </span>
        </div>
    </div>
    <hr>
    <div class="row">
        <div class="col-md-12">
            <h2 class="text-center text-info">Tournaments</h2>
            <table class="table table-striped table-inverse tour_table">
                <thead class="thead-inverse">
                    <tr>
                        <th>Name</th>
                        <th>Status</th>
                        <th>Winner</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each result}}
                        <tr>
                            <td><a class="tour" href="/tDetails/{{id}}">{{name}}</td></a>
                            <td>{{status}}</td>
                            <td>{{winner}}</td>
                        </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
    </div>
    <br>
    <br>
    <div class="t_list"></div>
{{>footer}}
